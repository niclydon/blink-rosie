# Reference repos — cloned locally, not vendored
refs/

# Blink APK extraction workspace — 100+ MB binaries, not vendored
blink-apk-staging/
*.apk
*.apkm
*.aab

# Captured artifacts that may contain account IDs, serials, tokens
logs/
*.pcap
*.pcapng
*.har

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/

# Go
*.test
*.out

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Secrets
.env
.env.*
!.env.example
*credentials*.json
tokens.json
tokens-mcp.json

# Remember plugin internals
.remember/logs/
.remember/tmp/

# BEGIN AI-CLI-WORKING-FOLDERS
# Local AI/CLI harness state — never commit (session, skills cache, scratch).
# Managed across all repos; keep in sync with ~/.config/git/ignore.
.claude/
.codex/
.codex
.grok/
.claire/
.antigravitycli/
.agents/
.kimi-code/
.kimi/
.remember/
.cursor/
.aider/
.continue/
.windsurf/
.opencode/
**/.claude/settings.local.json
# END AI-CLI-WORKING-FOLDERS
